{
  "name": "flavor-galaxy-corpus",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Recipe corpus for Flavor Galaxy: the seed recipes and the builder that maps their ingredients to canonical node indices. No server - recipe search is a static client-side index (see scripts/build-recipe-index.mjs).",
  "scripts": {
    "seed": "node scripts/build-seed.mjs"
  }
}
