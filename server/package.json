{
  "name": "flavor-galaxy-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Recipe search API for Flavor Galaxy. Serves recipe hits carrying precomputed canonical ingredient node indices.",
  "scripts": {
    "seed": "node scripts/build-seed.mjs",
    "dev": "npm run seed && tsx watch src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "index": "tsx scripts/build-index.ts"
  },
  "dependencies": {
    "@fastify/cors": "^10.0.1",
    "fastify": "^5.1.0",
    "meilisearch": "^0.45.0"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.6.3",
    "@types/node": "^22.9.0"
  }
}
